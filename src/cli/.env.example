# CLI Environment Variables
# Copy this file to .env and fill in your values

# gRPC target for the proxy server (API Gateway)
GRPC_TARGET=vertex-proxy-service-x.a.run.app:443

# API Key for authentication with the proxy server
API_KEY=your_api_key_here